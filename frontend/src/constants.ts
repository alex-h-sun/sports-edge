export const SPORT_LABELS: Record<string, string> = {
  nba: "NBA",
  nhl: "NHL",
  tennis: "Tennis",
};

// Substring-matched against each edge's market on the backend.
export const MARKET_OPTIONS = ["Moneyline", "Totals", "Spread", "Props"];
